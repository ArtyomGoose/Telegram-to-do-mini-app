{
  "buildCommand": "cd twa && npm install && npm run build",
  "outputDirectory": "twa/dist",
  "functions": {
    "twa/api/webhook.py": {
      "runtime": "python3.12"
    }
  },
  "rewrites": [
    { "source": "/api/webhook", "destination": "/twa/api/webhook.py" }
  ]
}
